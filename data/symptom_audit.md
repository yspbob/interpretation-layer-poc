# Symptom-test audit (selection rule item 5b) — 31 Aug 2026

Graded by three parallel model graders (fresh contexts, issue page + fixing-PR diff only), one grade per ticket. Grade key: MATCH = a test reproduces/asserts the reported symptom; PARTIAL = tests exercise changed or adjacent code but not the symptom itself; MISMATCH = tests unrelated (excluded by rule). Result: 25 MATCH, 4 PARTIAL, 0 MISMATCH, 0 UNVERIFIABLE. Under rule 5b only mismatches are excluded; PARTIAL grades are recorded and carried.

## Batch A (newest)

- issue=23043 pr=23055 grade=MATCH symptom: bulk-creating front ports with a range pattern mapped to multi-position rear ports wrongly rejected with position-count mismatch. test-evidence: test_forms.py test_front_port_position_count_valid validates the pattern form; test_views.py asserts bulk-created ports get correct rear-port slices.
- issue=22990 pr=23002 grade=MATCH symptom: bulk-edit forms listed nullable_fields not declared as fields, so fields like ModuleBayTemplate.position could not be edited/cleared. test-evidence: test_base_classes.py test_bulk_edit_nullable_fields asserts every nullable_fields entry is declared, across all bulk-edit forms; template testcases exercise the previously uneditable fields.
- issue=22985 pr=22986 grade=MATCH symptom: views rendering data-file contents sent no Cache-Control, letting browsers disk-cache sensitive content. test-evidence: core/tests/test_views.py test_content_is_not_cacheable fetches a secret-bearing DataFile view and asserts no-store; parallel tests cover config context, render-config, export templates.
- issue=22922 pr=22928 grade=MATCH symptom: handle_location_site_change() ran queries on the default DB instead of the saving connection under DB routing. test-evidence: dcim/tests/test_signals.py ScopeSignalConnectionTestCase asserts propagation queries pin to the saving connection; companions in ipam/virtualization.
- issue=22852 pr=22870 grade=MATCH symptom: scripts triggered via event rules notified on success even with notifications_default on_failure/never. test-evidence: test_event_rules.py test_eventrule_script_action_honors_script_defaults asserts NOTIFICATION_ON_FAILURE carried and no notification after success.
- issue=22812 pr=22867 grade=MATCH symptom: deleting a script with thousands of jobs loaded all jobs into memory (multi-GB spike). test-evidence: test_scripts_deletion.py reproduces 2,500-job scenario, asserts batched deletion and counting without instantiation.
- issue=22745 pr=22777 grade=MATCH symptom: scripts REST API allowed DELETE/PUT/PATCH with view-only permission or read-only tokens. test-evidence: test_api.py asserts 405 on modify methods and 403 for read-only tokens on unsafe methods.
- issue=22682 pr=22693 grade=MATCH symptom: deleting a SiteGroup cascade-deleted prefixes scoped to member sites via cache-only FKs. test-evidence: test_models.py asserts member-site prefix survives with _site_group_id nulled; analogues for Region/Location/Cluster/WirelessLAN; direct scopes still cascade.
- issue=22644 pr=22645 grade=MATCH symptom: front-to-rear port mappings produced no changelog record. test-evidence: test_port_mappings.py asserts CREATE/DELETE ObjectChange entries for mapping create, repoint, removal.
- issue=22588 pr=22684 grade=MATCH symptom: prefix scoped to a SiteGroup offered any VLAN instead of restricting to the group. test-evidence: test_forms.py asserts available_at_site_group dynamic param on the widget; test_filtersets.py asserts the filter returns only in-scope VLANs.

## Batch B

- issue=22578 pr=22606 grade=MATCH symptom: REST API returned other users' private SavedFilter/TableConfig objects the UI hides. test-evidence: test_api.py asserts private objects absent from list, 404 on retrieval, absent from GraphQL; owner access intact.
- issue=22531 pr=22542 grade=PARTIAL symptom: saved-filter combobox label unannounced by screen readers (duplicate element IDs broke label association). test-evidence: only test asserts element_id override in render_field_with_aria; never tests the selector page, duplicate-ID condition, or announcement itself.
- issue=22429 pr=22424 grade=MATCH symptom: constrained grant_token permissions ignored by REST API, allowing token creation violating the constraint. test-evidence: test_api.py asserts 403 for superuser-target creation under constraint, 201 for permitted; form and bulk-import companions.
- issue=22324 pr=22366 grade=MATCH symptom: GraphQL extra_choices filter declared flat while field stores nested pairs, so schema-compliant queries matched nothing. test-evidence: test_api.py asserts contains matches via GraphQL and nested operands rejected; test_lookups.py asserts choice_value matches values not labels.
- issue=22301 pr=22323 grade=MATCH symptom: two plugins with same-named taggable models crashed startup with Tag reverse-accessor clash. test-evidence: test_tags.py registers two same-named models in different apps, asserts clean system checks with distinct related names.
- issue=22273 pr=22401 grade=MATCH symptom: service with ~2000+ ports raised OperationalError (index row over 8191 bytes). test-evidence: test_models.py test_large_service saves a full-range service, directly reproducing the oversized-index scenario.
- issue=22237 pr=22385 grade=MATCH symptom: GET /extras/table-configs/add/ returned 500 (ObjectType.DoesNotExist on None object type). test-evidence: test_views.py asserts the standalone add view redirects with a warning instead of erroring; form constructible without table context.
- issue=22228 pr=22244 grade=MATCH symptom: inclusive vid_ranges undercounted _total_vlan_ids; singleton inclusive ranges stored 0 causing division-by-zero 500s. test-evidence: test_models.py asserts inclusive-range normalisation, singleton total of 1, utilization None instead of dividing by zero.
- issue=21763 pr=21778 grade=MATCH symptom: site edit page timed out with thousands of assigned ASNs. test-evidence: test_forms.py asserts at the 100-ASN threshold the multi-select is replaced by add/remove fields with correct M2M updates.
- issue=21498 pr=21815 grade=MATCH symptom: reterminating a cable raised CablePath DoesNotExist during event processing. test-evidence: test_models.py asserts event serialization no longer raises on the stale reference and the path accessor refreshes.

## Batch C (oldest)

- issue=21129 pr=21309 grade=MATCH symptom: deleting a scheduled job on a custom queue failed to cancel the RQ job, which later re-inserted itself. test-evidence: test_models.py asserts cancellation is fetched and invoked on the correct custom queue at delete.
- issue=20759 pr=20931 grade=PARTIAL symptom: long object-type names overflow the permission form selector. test-evidence: tests only assert the new widgets include/exclude selected values; truncation/overflow untested (fix lives in SCSS/TS).
- issue=20670 pr=20717 grade=MATCH symptom: device-type bulk import with wrongly-typed component data crashed with AttributeError instead of a friendly error. test-evidence: test_views.py posts exactly those malformed payloads and asserts the friendly per-record messages and numbering.
- issue=20497 pr=20514 grade=PARTIAL symptom: GraphQL vid_ranges contains with a scalar VID failed with a type-cast error. test-evidence: tests cover the new ORM lookup and filterset boundaries but never issue a GraphQL query where the failure was reported.
- issue=20474 pr=21740 grade=MATCH symptom: installing a single-{module}-placeholder module into a nested module bay failed with token-count mismatch. test-evidence: test_models.py (tagged regression #20474) builds the exact nested hierarchy and asserts the interface resolves without error.
- issue=20389 pr=20672 grade=MATCH symptom: bulk rename with select-all only renamed the current page. test-evidence: test_views.py creates 37 interfaces over page size 25, asserts 37 selected and all renamed.
- issue=19806 pr=19807 grade=MATCH symptom: a JobRunner could not mark a job failed; terminate() overwrote STATUS_FAILED. test-evidence: test_jobs.py runs a runner raising JobFailed and asserts final status FAILED.
- issue=19633 pr=19885 grade=PARTIAL symptom: a condition evaluated on None raised uncaught TypeError aborting all remaining event rules. test-evidence: tests assert controlled InvalidCondition on None/missing/wrong-typed values, but nothing exercises process_event_rules to show other rules survive.
- issue=18900 pr=19943 grade=MATCH symptom: REST paginator silently paginated unordered querysets. test-evidence: test_api.py asserts QuerySetNotOrdered raised on unordered querysets; ordered and iterable inputs unaffected.
