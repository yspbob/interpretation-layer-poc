# One development repair for the guidance assessor

15 September 2026. The user agreed to one focused repair cycle after the [failed qualification](FABLE-QUALIFICATION-RESULTS.md).

**The six model checks are complete.** The user explicitly approved the revised exports after the initial automatic approval rejection. See the [development result](FABLE-REPAIR-RESULTS.md): all six verdict sets were correct; five explanations passed under the original reading, with a narrower possible reading of the remaining sentence recorded separately. The original qualification result remains unchanged. The sections below preserve the preparation and agreed procedure.

## What changed

The [revised instructions](assessment-evidence-instructions-v2.txt) ask for a short explanation tied to a supplied passage or named observation. They distinguish evidence supporting a claim, evidence contradicting it under the same conditions, and evidence that cannot settle it. They also explain why a call into omitted code does not establish every effect of that code.

The selected reference packets now name each observed value and describe the test setup. One packet also includes the previously omitted implementation called by its wrapper. These are additions to the permitted evidence. The original source excerpts, candidate claims, reference requirements, expected verdicts and response schema are preserved.

The new packets are separate development artifacts. Nothing was written into the original qualification bank, answers or scoring record.

## The small development check

Six authored cases cover the observed scope errors, the code behaviour error, missing guidance and valid alternatives. They include two cases containing only valid alternative guidance, one empty guide and a false claim with an evidenced exception. The two cases about missing evidence also contain supported claims, so cautious review must not become indiscriminate rejection.

Each case gets one fresh Fable 5.1 High session through the established direct Claude Code route, with no tools. Only its permitted input enters the session. Expected answers, earlier responses, failure explanations and this selection record stay outside it. Use included Max allowance only, with no paid credits or API billing.

Preserve all six first responses before checking correctness. Do not coach, retry, or adjust later inputs after seeing an answer. Stop for a delivery or access failure. Every verdict and its evidence based reason must be correct for this development check to succeed. A repeated original error or a new unsupported assertion means the repair has not met that criterion.

A successful development check would justify preparing fresh qualification cases. It would not qualify the assessor. The next qualification retains the existing four acceptance requirements. Families Q02, Q03 and Q04 are now development material because their contents shaped this repair; their overlapping material must not qualify revised Astra roles either. The original Astra freeze remains intact as a historical artifact, but those families are no longer unused evidence for a revised configuration.

If this focused repair fails, stop extending the tuning loop and revisit the role. Fable may raise objections that we check against sources, but using it only in that capacity would require an explicit update to the trial assessment plan. Its scores must not silently become authoritative.

## Checks completed before any model call

All 29 observations in the affected reference packets were reproduced using the existing source matched packages. Every observed value matched the frozen record. Four deliberately wrong behaviour checks were detected. A separate local trace confirmed the side effect omitted from several earlier explanations.

All six inputs round trip without content changes, preserve the original candidates and requirements, and fit the previously exercised delivery size. The largest is 27,468 bytes, below 30,025 bytes. Four local audit checks passed: the revised input is accepted, changed instructions are rejected, the original auditor still rejects the revised instruction, and altered source content is rejected. These checks establish packet and audit behaviour, not model improvement.

The first preparation attempts encountered missing dependencies in two existing environments; the successful run used the assessment environment with the source matched evidence packages. An operator filename initially shadowed a Python standard library module and was renamed before execution. These failures happened before any development model submission and are recorded privately.

## Account check correction

While preparing the next run, we found cached or rate limited usage notices in 27 saved preflight records from the original batch. The earlier collection summary overstated how many account readings were fresh. Those records had numeric readings, including zero recorded credit spending, but that does not establish the current balance at every dispatch. The original records and correctness scores remain preserved.

For the development run, reject an incomplete, cached or rate limited allowance reading before sending a packet. Require a complete current view below 80% usage with no paid credit spending. Do not infer freshness from numeric fields alone. This is an account observation control, not a technical spending cap or proof of hidden provider activity.

## Preserved records

Six local tests also passed for the new account display reader, including cached readings with complete numeric values, missing credit information and paid spending.

Private packet freeze: `3ce3495814066b7f475ae037baf0a3b06636f71076cb9f35a3e9855665e2964d`.

Private execution record: `373be6abadb5bb53543ba1e554b0f8e3feb96a84e8fa086af1a2e04fb85f4bdf`.

Completed readiness manifest, including the account control addendum: `7c219d47422a38e9ed9ba44698418147b10087a55cffc25d9d4e9c0415929f50`.

The records preserve the six inputs, separate keys, selection and stop rule, evidence checks, revised instruction, original audit code and the development adapter. The original qualification and scoring commitments remain those in the result report.

The original next step was the six development responses after explicit export approval and successful live settings and allowance checks. That step is now complete. The next decision is the supplementary reviewer's role, as explained in the result report. No new qualification batch or paid spending is authorised by this document. Website wording and progress updates remain banked; the downloadable working plan records this decision.
