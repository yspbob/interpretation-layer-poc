# Capped screening result: neither task qualified

16 September 2026. The approved allocation completed two unguided first attempts in the declared order. Both passed the fixed checks. The two-screen cap is exhausted and the allocation is closed: no neutral guide or fresh comparison was run.

| Screen | Common acceptance methods | Original relevant regressions | Eligible material failure |
|---|---:|---:|---|
| First task | 11/11 passed | 199 passed | None observed |
| Second task | 12/12 passed | 199 passed | None observed |

Both basic happy paths passed. Each regression result includes the same one upstream skip and two exclusions declared before execution; all 115 original test functions were preserved. Each attempt completed within its ten-minute supervised limit using the same Astra High setting. Additional agent-authored tests are retained but their differing counts are not comparative quality scores. No output was repaired after evaluation.

## What this means

The capped search found no eligible task on which to run the proposed guidance comparison. It does not establish representative unguided reliability, equivalence of guided and unguided coding, or the absence of guidance value on other work. It also supplies no positive evidence of guidance benefit. Preserve the earlier FC-01 null comparison separately.

The tasks were newly specified requests against an already familiar pinned repository, with explicit requirements. The rationale that they involved several existing behaviours did not produce a failure in either observed attempt. A finite test suite cannot establish complete correctness, and these were not sampled representative production tasks. No full upstream suite, strict typing, lint or performance claim is made.

## Controls and limitations

The input templates, task text and evaluator were fixed before model outputs. Both attempts used fresh contexts with no conversation fork, pristine source copies and the same coding instructions and environment. The evaluator used original upstream tests/configuration independently of candidate test edits. Original session records, code, changes, inputs, initial contexts, first final responses, checks, usage and selection decisions are retained privately.

The initial contexts contained general parent-project instructions. Neither included the current private task/evaluator details or other implementation. Recorded commands showed no study/evaluator/other-role or network access. Tools were not OS-confined, and the selecting investigator performed the source/input/evaluator review; no independently qualified assessor or certified isolation is claimed. A local final-message extraction mismatch was corrected using the already preserved session; it did not alter an experimental input, output or test.

The user limited spending to the then-available credits. The earlier 5% stopping preference was retained as a reserve, all observed balances remained above it, and no top-up, reset redemption or API route was used. All screening/preparation effort remains part of the cost. Account observations cannot attribute an exact price to individual sessions; operational amounts remain private.

## Closed allocation and next decision

Two screens completed; the conditional guide and two comparator slots are unrun because neither task qualified. Do not extend this cap, add a third task, change the scoring, retry or reopen qualification automatically. Stop and review whether any further experiment is justified before proposing a different task class and a new bounded allocation.

Private records are in exploratory-screening-2026-09-16/run-2026-09-16/. The unchanged preparation freeze is 034b7f114f94d730ff78bcd84e7c6c2a0cdfe8b049605bcd3ce57c411ef50858. The 41-record completion freeze is 9c6e08fc9aff8f874ee534482ae3a835e2238eac5f71094f7b7407b799e780d3. Neither raw task content nor evaluator fixtures are published here.
