# Supplementary audit sample: model_refs (fact graph v2, plan v1.2 item F11)

Drawn 3 Sep 2026 with `random.Random(20260903).sample(rows, 8)` over `SELECT rowid,* FROM model_refs ORDER BY module, lineno` (391 rows; seed = ratification date of plan v1.2). Each fact is checked against the frozen copy at T0 (`ea4c205`): the window column reproduces the four lines the command prints, so the check can be made by eye; running the command confirms the window is genuine. Owner marks each fact PASS or FAIL; this table is the Gate 2 supplement for the v2 table.

| # | Fact (module, class.field, kind, target, cross-app) | Verify at T0 (prints a 4-line window) | Must appear in window | Window at T0 | Result |
|---|---|---|---|---|---|
| 1 | `extras.models.notifications` NotificationGroup.groups ManyToManyField -> `users.Group` (app `users`, cross_app=1) | `git show ea4c205:netbox/extras/models/notifications.py \| sed -n '135,138p'` | `groups` and `'users.Group'` | `135:     groups = models.ManyToManyField(`<br>`136:         to='users.Group',`<br>`137:         verbose_name=_('groups'),`<br>`138:         blank=True,` | |
| 2 | `dcim.models.devices` VirtualChassis.master OneToOneField -> `Device` (app `dcim`, cross_app=0) | `git show ea4c205:netbox/dcim/models/devices.py \| sed -n '1062,1065p'` | `master` and `'Device'` | `1062:     master = models.OneToOneField(`<br>`1063:         to='Device',`<br>`1064:         on_delete=models.PROTECT,`<br>`1065:         related_name='vc_master_for',` | |
| 3 | `dcim.models.cables` CableTermination._rack ForeignKey -> `dcim.Rack` (app `dcim`, cross_app=0) | `git show ea4c205:netbox/dcim/models/cables.py \| sed -n '280,283p'` | `_rack` and `'dcim.Rack'` | `280:     _rack = models.ForeignKey(`<br>`281:         to='dcim.Rack',`<br>`282:         on_delete=models.CASCADE,`<br>`283:         blank=True,` | |
| 4 | `utilities.tables` (call) get_model -> `extras.TableConfig` (app `extras`, cross_app=1) | `git show ea4c205:netbox/utilities/tables.py \| sed -n '22,25p'` | `get_model('extras', 'TableConfig')` | `22:     TableConfig = apps.get_model('extras', 'TableConfig')`<br>`23:     return TableConfig.objects.filter(`<br>`24:         Q(shared=True) \| Q(user=user if user.is_authenticated else None),`<br>`25:         object_type=ObjectType.objects.get_for_model(table.Meta.model),` | |
| 5 | `dcim.models.device_components` InventoryItem.manufacturer ForeignKey -> `dcim.Manufacturer` (app `dcim`, cross_app=0) | `git show ea4c205:netbox/dcim/models/device_components.py \| sed -n '1306,1309p'` | `manufacturer` and `'dcim.Manufacturer'` | `1306:     manufacturer = models.ForeignKey(`<br>`1307:         to='dcim.Manufacturer',`<br>`1308:         on_delete=models.PROTECT,`<br>`1309:         related_name='inventory_items',` | |
| 6 | `extras.models.tags` Tag.object_types ManyToManyField -> `core.ObjectType` (app `core`, cross_app=1) | `git show ea4c205:netbox/extras/models/tags.py \| sed -n '37,40p'` | `object_types` and `'core.ObjectType'` | `37:     object_types = models.ManyToManyField(`<br>`38:         to='core.ObjectType',`<br>`39:         related_name='+',`<br>`40:         blank=True,` | |
| 7 | `dcim.models.sites` Site.region ForeignKey -> `dcim.Region` (app `dcim`, cross_app=0) | `git show ea4c205:netbox/dcim/models/sites.py \| sed -n '159,162p'` | `region` and `'dcim.Region'` | `159:     region = models.ForeignKey(`<br>`160:         to='dcim.Region',`<br>`161:         on_delete=models.SET_NULL,`<br>`162:         related_name='sites',` | |
| 8 | `dcim.models.devices` Device.device_type ForeignKey -> `dcim.DeviceType` (app `dcim`, cross_app=0) | `git show ea4c205:netbox/dcim/models/devices.py \| sed -n '451,454p'` | `device_type` and `'dcim.DeviceType'` | `451:     device_type = models.ForeignKey(`<br>`452:         to='dcim.DeviceType',`<br>`453:         on_delete=models.PROTECT,`<br>`454:         related_name='instances'` | |

Totals at T0: 391 string references, 186 cross-app (`SELECT COUNT(*) FROM model_refs WHERE cross_app=1`). Complementary to the 3,155 import rows: a `ForeignKey('ipam.VLAN')` in `dcim` is a dependency no import statement records.
